---
layout: post
title: "Linux 기본 명령어 (K-shield jr.)"
date: 2026-06-26 00:00:00 +0900
categories:
  - security
tags:
  - k-shield-junior
  - linux
notion_url: "https://app.notion.com/p/3764b27666b080938050d2c76192d4c7"
---
## 시스템 즉시 종료 및 재시작

- 시스템 즉시 종료

```bash
shutdown -h now
halt
init 0
```

- 즉시 재시작

```bash
shutdown -r now
reboot
init 6
```

- `shutdown` 명령 옵션

```bash
shutdown -h 10m    # 10분 뒤에 종료
shutdown -r 22:00  # 종료 후 오후 10시 재가동
shutdown -c        # 예약된 종료 취소
```

---

## 파일 및 디렉터리 목록 보기

```bash
ls
```

| 옵션 | 기능 |
| --- | --- |
| `-a` | 디렉터리 내의 모든 파일 출력, 숨김 파일 포함 |
| `-l` | 파일 정보를 표시 |
| `-s` | 블록 단위로 파일 크기 표시 |
| `-t` | 파일 최근 변경 시간에 따라 정렬해서 출력 |
| `--color` | 파일 종류에 따라 색상 표시 |
| `-R` | 현재 작업 디렉터리와 하위 디렉터리 모두 출력 |
| `--help` | 도움말 |

---

## 현재 디렉터리 위치 출력

```bash
pwd
```

## 현재 디렉터리 변경

- 절대 경로 이용

```bash
cd /home/user/Desktop
```

- 상대 경로 이용

```bash
cd ./user/Desktop
```

`.`으로 시작하거나 `filename`으로 시작하면 상대 경로이다.

---

## 파일 혹은 디렉터리 복사

```bash
cp [option] [origin-file] [Path and FileName]
cp [option] [origin-Directory] [Path and Directory]
```

| 옵션 | 기능 |
| --- | --- |
| `-a` | 원본 파일의 속성, 링크 정보들을 유지하면서 복사 |
| `-b` | 백업 파일을 만듦 |
| `-d` | 심볼릭 파일 자체를 심볼릭 정보와 함께 복사 |
| `-f` | 복사할 파일이 존재할 때 삭제하고 복사 |
| `-p` | 원본 파일의 소유, 그룹, 권한, 허용 시간을 보존한 채로 복사 |
| `-r` | 서브디렉터리 내에 있는 모든 파일을 통째로 복사 |
| `-u` | 대상 파일보다 원본 파일이 새로운 것일 때 복사 |

---

## 파일 삭제

```bash
rm [option] [FileName]
```

| 옵션 | 기능 |
| --- | --- |
| `-d` | 디렉터리 삭제 |
| `-f` | 삭제할 것인가라는 메시지 없이 그대로 강제 삭제 |
| `-i` | 삭제 시 일일이 삭제할 것인지 물음 |
| `-r` | 하위 디렉터리를 포함한 파일들을 모두 삭제 |
| `-v` | 삭제되기 전의 파일들을 보여줌 |

---

## 디렉터리 생성 및 삭제

- `mkdir`

```bash
mkdir [option] [FileName]
```

- `rmdir`

```bash
rmdir [option] [FileName]
```

| 옵션 | 기능 |
| --- | --- |
| `-p` | 지정한 하위 디렉터리까지 한 번에 생성 |

---

## 파일 이동 및 이름 변경

- 파일 이동

```bash
mv [원본 파일] [이동하려는 경로 및 저장할 파일명]
```

- 파일 이름 변경

```bash
mv [원본 파일] [변경하려는 파일명]
```

---

## 파일 찾기

```bash
find [Path] [표현식] [지정 조건]
```

| 표현식 | 기능 |
| --- | --- |
| `-name` | 지정된 문자열 패턴에 해당하는 파일 검색 |
| `-empty` | 빈 디렉터리 또는 크기가 0인 파일 검색 |
| `-delete` | 검색된 파일 또는 디렉터리 삭제 |
| `-exec` | 검색된 파일에 대해 지정된 명령 실행 |
| `-path` | 지정된 문자열 패턴에 해당하는 경로에서 검색 |
| `-type` | 지정된 파일 타입에 해당하는 파일 검색 |
| `-atime` | 파일 접근 시각을 기준으로 파일 검색 |
| `-ctime` | 파일 수정 시각을 기준으로 파일 검색 |
| `-mtime` | 파일 생성 시각을 기준으로 파일 검색 |

---

## 프로세스 상태 확인

```bash
ps [option]
```

| 옵션 | 기능 |
| --- | --- |
| `-ef` | 표준 구문으로 시스템의 모든 프로세스 표시 |
| `-u` | 실행한 유저와 실행 시간을 표시 |
| `-j` | job 형식으로 표시 |
| `-l` | 긴 포맷으로 출력 |
| `-m` | 메모리 정보 표시 |
| `-a` | 다른 유저의 프로세스 현황 표시 |
| `-x` | 터미널 제어 없이 프로세스 현황 표시 |

---

## 프로세스 종료

```bash
kill [option] [PID]
```

---

## 파일 내용 출력

```bash
cat [option] [FileName]
```

---

## 파일, 명령 결과 내 특정 패턴 검색

```bash
grep [option] [표현 파일명 혹은 문자열]
```

| 옵션 | 기능 |
| --- | --- |
| `-v` | 일치하는 내용이 없는 라인을 표시 |
| `-c` | 일치하는 내용이 있는 행의 개수를 센다 |
| `-l` | 일치되는 내용이 있는 파일의 이름만 표시 |

---

## 긴 내용의 파일 내용, 명령 결과 확인

```bash
more [option] [cmd] | more
```

---

## 이전 명령어 목록 보기 및 실행

```bash
history [option]
```

---

## 디스크 용량 및 정보 확인

```bash
df [option]   # 하드 디스크의 용량 확인
du [option]   # 사용한 디스크 용량에 대한 정보 제공
```

---

## 블록 장치 연결

파일 시스템을 사용하기 위해 마운트시키는 기능이다.

```bash
mount [-fnrvw] [-t fs 유형] [-a 옵션] 장치 디렉터리
```

---

## 파일 시스템 만들기

```bash
mkfs [-t 형태] [-fs 옵션] 장치명 [블록]
```
